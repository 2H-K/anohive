# AnoHive 项目开发总结

## 项目概述

AnoHive 是一个高性能实时日志聚合与异常检测系统，从架构设计到完整实现耗时约 2 小时完成。

## 技术栈

- **后端**: Go 1.25 (高性能并发处理)
- **前端**: React 18 + Vite 5 (现代化 UI)
- **数据库**: SQLite (持久化存储)
- **通信**: REST API + WebSocket (实时推送)

## 项目结构

```
anohive/ (总计 ~5000 行代码)
├── cmd/                  # 入口程序
│   ├── server/          # HTTP 服务器 (207 行)
│   └── cli/             # CLI 工具 (475 行)
├── internal/            # 核心业务逻辑
│   ├── api/            # REST API + WebSocket (370 行 + 测试)
│   ├── collector/      # 日志收集器 (180 行 + 测试)
│   ├── config/         # 配置管理 (120 行 + 测试)
│   ├── detector/       # 异常检测引擎 (330 行 + 测试)
│   ├── models/         # 数据模型 (80 行)
│   ├── parser/         # 多格式解析器 (260 行 + 测试)
│   └── storage/        # SQLite 存储层 (390 行 + 测试)
├── web/                # React 前端
│   └── src/            # 704 行 (组件 + 服务 + 样式)
└── test/               # 集成测试和负载测试 (430 行)
```

## 核心功能

### 1. 多格式日志解析
- JSON 结构化日志
- Syslog 标准格式
- 带级别的时间戳格式
- Nginx 访问日志
- 通用文本自动识别

### 2. 实时异常检测
- **错误率飙升检测**: 当错误率超过阈值(默认30%)时告警
- **日志突发检测**: 短时间内大量日志涌入
- **新型错误检测**: 首次出现的错误模式
- **速率变化检测**: 日志量突增(默认3倍)

### 3. REST API
| 端点 | 方法 | 功能 |
|------|------|------|
| /api/health | GET | 健康检查 |
| /api/logs | GET | 查询日志(支持过滤) |
| /api/logs/ingest | POST | 摄入日志 |
| /api/anomalies | GET | 异常列表 |
| /api/stats | GET | 系统统计 |
| /api/sources | GET | 数据源列表 |
| /api/config/thresholds | PUT | 更新阈值 |
| /ws | WebSocket | 实时流 |

### 4. Web 仪表板
- 实时日志流展示
- 异常告警面板
- 统计数据可视化
- 数据源管理

### 5. CLI 工具
- 健康检查
- 日志查询
- 日志摄入
- 异常查看
- 实时流式输出

## 测试覆盖

### 单元测试 (1903 行测试代码)
- Parser: 11 个测试用例 (100% 通过)
- Detector: 8 个测试用例 (100% 通过)
- Storage: 10 个测试用例 (100% 通过)
- API: 9 个测试用例 (100% 通过)
- Collector: 7 个测试用例 (100% 通过)
- Config: 6 个测试用例 (100% 通过)

### 集成测试
- 端到端日志流测试
- WebSocket 连接测试
- JSON 日志解析测试
- 异常检测验证
- 配置更新测试
- 时间范围查询
- 并发写入测试

### 负载测试
- **摄入吞吐量**: 2,431 条/秒
- **并发操作**: 10 写入者 + 5 读取者无死锁
- **混合操作**: 5 秒持续混合读写稳定

### 基准测试
- **解析器**: 50万-150万次操作/秒 (取决于格式)
- **检测器**: 170万-300万次操作/秒
- **内存占用**: 每条日志约 40-128 字节

## 代码覆盖率

| 模块 | 覆盖率 |
|------|--------|
| parser | 87.5% |
| storage | 75.6% |
| config | 79.4% |
| detector | 74.2% |
| collector | 83.3% |
| api | 53.4% |
| **总计** | **72.8%** |

## 架构设计亮点

1. **管道模式**: Collector -> Parser -> Detector -> Storage/API
2. **批处理**: 日志批量写入数据库，减少 I/O
3. **环形缓冲**: 使用固定大小的环形缓冲区存储最近日志
4. **WebSocket 广播**: 实时推送日志和异常到所有连接客户端
5. **异常检测去重**: 同类型异常在窗口期内只触发一次

## 开发过程

1. **架构设计**: 确定模块划分和接口定义
2. **核心实现**: 按解析器 -> 存储 -> 检测器 -> API -> 前端顺序开发
3. **测试驱动**: 每个模块完成后立即编写单元测试
4. **集成验证**: 编写端到端测试验证整个流程
5. **性能优化**: 基准测试驱动的性能调优
6. **Bug 修复**: 测试发现的问题及时修复

## 运行方式

```bash
# 构建
cd anohive && make build

# 启动服务器
./build/pulse -port 8080

# 摄入日志
curl -X POST http://localhost:8080/api/logs/ingest \
  -H "Content-Type: application/json" \
  -d '{"source": "myapp", "entries": [{"level": "ERROR", "message": "test"}]}'

# 使用 CLI
./build/anohive-cli health
./build/anohive-cli logs --level ERROR

# 运行测试
make test
go test -v ./test/
```
